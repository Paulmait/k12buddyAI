# ============================================
# K12Buddy Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local or any file with real secrets!

# ===========================================
# CLIENT-SIDE (Safe to expose in mobile app)
# ===========================================
# These are public keys that can be bundled with the app
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ===========================================
# SERVER-SIDE ONLY (NEVER expose these!)
# ===========================================
# These must ONLY be used in Edge Functions or server-side code
# They bypass Row Level Security and have full database access

# Supabase service role key - NEVER expose to client
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AI Provider API Keys - NEVER expose to client
# These are set in Supabase Dashboard > Edge Functions > Secrets
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# ===========================================
# DEVELOPMENT/TESTING
# ===========================================
# For local Supabase development
# SUPABASE_URL=http://localhost:54321
# SUPABASE_ANON_KEY=local-anon-key
# SUPABASE_SERVICE_ROLE_KEY=local-service-role-key
